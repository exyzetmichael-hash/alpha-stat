name: Пинг базы данных (чтобы не засыпала)

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Запрос к /api/health
        run: |
          curl -fsS --max-time 20 "${{ vars.HEALTH_CHECK_URL }}/api/health" || exit 1
